闭包：所有对象都是一个闭包，在一个函数里返回另一个函数会形成一个闭包，闭包可以让内部的变量供外部使用，拓宽作用域，应用场景：需要使用私有变量，
跨域： 同源策略，同协议，同端口，同域名，跨域解决方案有，jsonp,利用script不受同源策略限制；cors,
http响应码：2xx, 成功接收
           3xx, 重定向
           4xx, 系统异常
           5xx,找不到服务器