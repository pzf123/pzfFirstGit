在代码目录下建立.gitignore文件： .gitignore （用记事本另存可以保存这个名字），
配置上要忽略的文件或者文件夹，然后提交到git 上即可。
npm install -S webpack webpack-cli
//在package.json
"scripts": {
    "dev": "webpack-dev-server"
  },
  npm install webpack-dev-server
  输入npm run dev